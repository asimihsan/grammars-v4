# Antlr grammar for AWS Athena

https://docs.aws.amazon.com/athena/latest/ug/ddl-sql-reference.html

Examples that should parse live in `examples/`. For negative cases, add a matching `.errors` sidecar (e.g., `bad.sql.errors`) in `examples/`.
